const express=require("express");const cors=require("cors");const kuromoji=require("kuromoji");const path=require("path");const app=express();app.use(cors());app.use(express.json());let t=null;const init=()=>new Promise((res,rej)=>{if(t)return res(t);kuromoji.builder({dicPath:path.join(__dirname,"node_modules","kuromoji","dict")}).build((e,tk)=>{if(e)rej(e);else{console.log("Kuromoji loaded");t=tk;res(tk);}});});init();app.post("/analyze",async(req,res)=>{try{const tk=await init();const r=tk.tokenize(req.body.text||"");res.json(r.map((x,i)=>({id:String(i),surface:x.surface_form,reading:x.reading||undefined,lemma:x.basic_form,posDetail:x.pos_detail_1,pos:x.pos==="助詞"?"particle":(x.pos==="名詞"?"noun":(x.pos==="動詞"?"verb":(x.pos==="助動詞"?"auxiliary":"other"))),isParticle:x.pos==="助詞"})));}catch(e){res.status(500).json({error:e.message});}});const PORT=process.env.PORT||3000;app.listen(PORT,()=>console.log("API on "+PORT));